type SinglePatientPayload {
    status: Boolean!
    error: String
    object: Patient!
}


type MultiplePatientsPayload {
    status: Boolean!
    error: String
    object: [Patient]!
}

type StatusPayload {
    status: Boolean!
    error: String!
}
